# iPhone 15 クラッシュの根本原因と改善計画（2026-07-25）

> **【実施済み・2026-07-25】第0段〜第7段をすべて実装し、デプロイ完了（本番 `assets/proto3d-Cix1OmoN.js`／CI success）。**
> **残るのは作者による実機確認だけ（第10節の順路）。実施結果は末尾「14. 実施の結果」を参照。**

7つの役割で並行調査した結果の統合版。**次のセッション（/goal）はこの文書を読んでから着手すること。**
各班の詳細報告は本文末尾「調査の元資料」を参照。

---

## 0. 結論（先に）

**原因は2つ。どちらもメモリの漏れ／二重確保で、どちらもコードで確認済み。**

| # | 真因 | 増え方 | 場所 |
|---|---|---|---|
| **A** | 住人VRMを準備するたびにGPUテクスチャが捨てられずに残る | **上限なし**（1体あたり約5.6枚≒1.9MB、2地点往復で約20MB/周） | `main.js:17692 / 17699-17703 / 16703 / 17756 / 17767` |
| **B** | ばしょマップが2つ目のWebGLレンダラーでシーン全体を描き直す | 開いた瞬間に**+71.3MB（GL呼び出しを直接計装した実測値）**、閉じても1バイトも返らない | `main.js:19619 / 19744 / 19752 / 19803 / 19864` |

土台の常駐が約815〜860MB。そこにAが歩くたび積み上がり、Bを開けば一撃で+71.3MB。
iOSのWebページの上限は実測で約2GB（`ActiveHard 2048 MB`。RAM 12GB機でも同値＝端末のRAMでは決まらない）。越えた時点でiOSがページのプロセスごと殺す＝**「何の前触れもなく突然、読み込み直される」**。

**A は数行で止まる。B は半日。この2つで、ふつうに歩くぶんには落ちなくなる公算が大きい。**

---

## 1. 症状（本人確認済み・2026-07-25）

- 端末：iPhone 15 無印（A16 / RAM 6GB）、**iOS 26系**、**ホーム画面に追加したアイコンから全画面（standalone）**で起動
- タイトルは出る。「はじめる」も通る。**数秒〜数十秒ちゃんと遊べたあと**、突然ページが読み込み直される。延々繰り返す
- **落ちる直前のお知らせ「画面が とまっちゃった…すぐ もどすね。」は見ていない**
- **「かるい『安全モード』で 起動中…」も、避難画面も、一度も見たことがない**
- 落ちる状況に規則性は感じていない
- iPhone 17 Pro では起きない

### 切り分けの結論
- `showToast` は巻き上げされる関数宣言（`main.js:14142`）＝コンテキスト喪失が起きていればお知らせは必ず出る。出ていない
  → **WebGLコンテキストの喪失ではなく、iOSによるプロセスの強制終了（メモリ超過）**
- `main.js:19` 相当の standalone 判定でふるまいを変えている箇所は **0件**＝開き方ではゲーム側の挙動は変わらない
  （ただしSafariのタブ版とのA/Bは、コストゼロなので実機で一度試す価値あり）

---

## 2. 真因A：住人VRMの準備でGPUテクスチャが漏れる

### 筋道（4箇所が噛み合っている）
1. `main.js:17686-17698` — 準備処理が、**加工前の元テクスチャ**を `texList` に集める（ついでに256pxへ縮小）
2. `main.js:17699-17703` — 役づくり（`girlize` / `boyize` / `yukataize` …）が走る
3. `main.js:16697-16703` — その中の `texCanvas(m).apply()` が
   `m.map = new THREE.CanvasTexture(cv)` と**新しいテクスチャに差し替え、元を `dispose()` しない**
   → `texList` の中身は**もう誰も参照していない孤児**になる
4. `main.js:17756` — `for (const t of texList) renderer.initTexture(t)` で、**その孤児をGPUへアップロードする**
5. `main.js:17767` の `deepDispose` は**シーングラフしか歩かない**ので、孤児には永久に到達しない

### 実測（2班が独立に確認）
- 対照実験：VRM ON → `renderer.info.memory.textures` が **183 → 720枚**、全員を捨てた後も **626枚**。`gc()` でも戻らない
  VRM OFF → **150 → 185枚で頭打ち**。シーンから到達可能なのは常に343枚 ＝ **約280枚が孤児**
- 別班の周回試験：同じ2地点を往復するだけで 150→276→367→447枚（**+20MB/周・上限なし**）
- **その場に立ち止まれば90秒間まったく増えない**＝「歩くと増える／止まれば増えない」＝典型的な漏れの形
- 1体の準備で +約24枚。必要なのは約18枚。**差の約5.6枚が毎回捨てられずに残っている**

### 直し方（推奨）
`texCanvas().apply()` で差し替える前の `m.map` / `m.shadeMultiplyTexture` を `dispose()` する。
または `texList` への収集を `-ize` の**後**へ移す（＝生き残っている方だけを先上げする）。
**どちらも数行。まずこれ。**

---

## 3. 真因B：ばしょマップが2つ目のWebGLレンダラーでシーンを描く

```
main.js:19619  let baseCanvas = null, glr = null, orthoCam = null
main.js:19744  if (!glr) { glr = new THREE.WebGLRenderer({ antialias: true, preserveDrawingBuffer: true }); ... }
main.js:19752  glr.render(scene, orthoCam)          ← シーン全体をもう一度描く
main.js:19803  function openMap() { ... renderBase(); ... }   ← 開くたびに呼ばれる
main.js:19864  マップを開いたまま画面が回転すると、また呼ばれる
```
- `glr` は**一度も `dispose()` されない**（`glr` の出現は上記6箇所のみ）
- **WebGLはコンテキスト間でGPU資源を共有しない**（three.js r169 実ソースで確認。`initGLContext()` が properties / textures / attributes / geometries / programCache をレンダラごとに新規生成）
  → 開いた瞬間に、世界のジオメトリとテクスチャがGPUへ二重にアップロードされ、シェーダーも再コンパイルされ、そのまま常駐する
- **実測（`bufferData` / `texImage2D` / `texStorage2D` / `renderbufferStorage` と `delete*` を直接計装）**：
  開く前 tex 127.02MB / buf 50.36MB → 開いた後 tex 188.63MB / buf 60.08MB ＝ **+71.33MB**。**閉じても1バイトも返らない**。WebGLコンテキスト数 2→3
  （当初「+275MB」と見積もったが、GPUへは実際に描いた分しか上がらないため過大だった。**正しくは +71.3MB**）
- `scene.fog = null` ＋ 世界全体を覆うオルソカメラ＝**霧カリングもフラスタムカリングも効かず、文字どおり全部を描く**
- `preserveDrawingBuffer: true`（メイン側では発熱対策でわざわざ廃止した設定）
- **ばしょマップは毎ロードON確定**（`main.js:18673`）＝実際に開かれる
- 起動時には走らない（呼び出し元は openMap と resize の2箇所だけ）＝**歩いて積み上がるAが主犯、Bは開いた時の一撃**

### いちばん安い直し方（まずこれ・3行・見た目の犠牲ゼロ）
`baseCanvas` へ写し取った直後（`main.js:19756` の次）に
```
glr.dispose(); glr.forceContextLoss(); glr = null
```
＝開くたびに作り直しになるが、**常駐しなくなる**。ただし開くたびに山が立つので、根治は下記の案(a)。

### 副作用（重要）
過去に「**CPU側の頂点配列を解放したら"ばしょマップ"が壊れた（＝禁じ手）**」という事故の**正体がこれ**。
第2のコンテキストへ上げ直すために、CPU側の元データが必要だった。
→ **Bを作り直せば、幾何のCPU配列（191.8MB）を解放する扉が開く。**

### 直し方（推奨＝案(a)）
メインのレンダラーで `WebGLRenderTarget` へ**タイトル表示中に一度だけ**焼き、`readRenderTargetPixels` → `baseCanvas`、RTは即 `dispose()`。
- 第2コンテキストを廃止（+275MB を封鎖）
- `tday` / `dayAuto` を一時的に差し替えて戻す荒業（`19749-19753`）が丸ごと不要になる
- メインのコンテキストで描くので、CPU配列が無くても描ける
- 罠：`readRenderTargetPixels` は左下原点＝**上下反転が必要**。焼くのは初回描画の後（`markTitleReady` 直後）
- 却下：(b) ビルド時に焼く＝「焼き忘れ＝直したのに地図が古い」運用事故／(c) 毎回 dispose＝開くたび+275MBの山が立ち、iPhone 15はその一瞬で死ぬので応急にすらならない
  （低メモリ段での応急は「`renderBase()` を飛ばして `19762` の無地フォールバックを使う」が正解）

---

## 4. 安全装置が死んでいた（確定）

`main.js:18184`
```
setTimeout(() => { localStorage.setItem('hn3d_bootn', '0') }, 8000)
```
- 起点が `warmGpuStaged()` 完了直後。**同じ8秒で「はじめる」が押せるようになる**（`18183`）
- ＝ **「はじめる」を押す前に、必ずカウンタが0に戻っている**
- 実測：タイトル表示中に既に0、「はじめる」直後も0
- よって `_bn >= 2`（安全モード）も `_bn >= 4`（避難画面）も**一度も発動しない完全な死にコード**
- さらに `main.js:77` の `visibilitychange:hidden` は、**他アプリへの切替や画面ロックでも**0に戻す誤リセット

本人の証言（お知らせも避難画面も見たことがない）と完全に一致。
**効く薬（安全モード＝VRM全停止）は既に書かれていた。飲ませる引き金だけが壊れていた。**

---

## 5. 落ちるたびに、その日の散歩が全部消えている

`main.js:14235`
```
function saveState() { localStorage.setItem('hn3d_state', JSON.stringify({ v, day, flags, caught, fish })) }
```
- **現在地とゲーム内時刻は保存対象に入っていない**
- 呼び出しは3箇所だけ：新しい日（`14017`）／`visibilitychange:hidden`（`14248`）／`pagehide`（`14249`）
- **プロセスを強制終了されると、この3つはどれも発火しない**
- ＝ 二ツ池の夕方で落ちる → 次はマンション前の朝から。捕まえた虫も魚も消える
- 位置の自動保存は存在しない（`hn3d_spawn` は手動保存のみ・`main.js:9142, 18894`）

**落ちること自体を直すのとは別に、ここは必ず救う。**

---

## 6. メモリの内訳（実測・ヘッドレス）

| 区分 | 起動直後 | 遊んだ後 |
|---|---:|---:|
| JSヒープ（GC後の谷） | 415MB | 435MB |
| ジオメトリのCPU配列 | 191.8MB | 191.8MB |
| GPU頂点バッファ（歩くほど遅延アップロード） | 47.4MB | → 最大 191.8MB |
| GPUテクスチャ（世界28＋主人公VRM65.4＋住人0） | 93MB | 49＋65.4＋50〜160MB |
| **GPUテクスチャの解放漏れ（真因A）** | 0 | **20MB × 入替回数（上限なし）** |
| レンダーターゲット・深度（影2048²=16MB、Composer RGBA16F×2=10.3MB） | 34MB | 34MB |
| canvas の裏画像 | 33.7MB | 33.7MB |
| **合計** | **約815MB** | **約1,060MB ＋ 入替回数×20MB** |

- **JSヒープのリークは無い**（90秒静置で全指標が完全に横ばい）。増加は「移動＝新しい景色を描いたこと」に完全に紐づく
- **軽量モードは半分しか効いていない**：VRM停止でテクスチャ −55MB は効くが、**世界のジオメトリを1バイトも減らしていない**（191.63→187.20MB、−2%）。影マップ16MBとComposer RGBA16F 10.3MBも据え置き
  → **「安全モードでも落ちる」は仕様どおり**

### 削減の見通し（差分は実測、絶対値は目安）
```
現状 820〜860MB
 → 真因Aを止める            ：頭打ちになる（無限増加が消える）
 → 真因B（マップ）＋CPU配列解放：630〜670MB   （工数 約2日）
 → KEEP 20→8・主人公VRM 512・影マップ1024：545〜590MB （工数 約2日）
 → 旧プロト3エリアを作らない ：460〜500MB   ※要・本人確認（下記）
 → 看板canvasの縮小と統合   ：435〜475MB
```
目標＝**常駐600MB台・余白300MB以上**。

---

## 7. 段取り（タスク一覧）

### 第0段：実機で測れるようにする（最優先・約141行）
- **クラッシュのパンくず**：1秒ごとに `localStorage['hn3d_bc']` へ上書き。`pagehide` で「無事に終わった印（ok）」を書く。次回起動時に印が無ければ＝前回は落ちた
- **主指標＝`renderer.info.memory.textures` の「今」と「起動時」と「シーンから到達可能な数」**。
  **`tex − texR` ＝ まいご（孤児）の枚数。作者が見るべき唯一の数字**
- 併記：ジオメトリ数／プログラム数／自前バイト数／のべVRM準備体数／resize累計／出来事リング（直近8件）／`gl`（コンテキスト喪失の有無）
- **開発者向けHUD**（せってい→🛠→「かずを 画面に 出す」）で歩きながら見られるように
- 次回起動時の見せ方は素朴に：「前回は ○分○秒あそんだところで とまっちゃったみたい」。数字は開発者向けの場所へたたむ
- → **これで「直ったか」を実機で判定できるようになる。以降の全作業の物差し。**

### 第1段：漏れを止める（数行）
- 真因A。`texCanvas().apply()` で旧テクスチャを `dispose()`、または `texList` の収集を `-ize` の後へ

### 第2段：ばしょマップの作り直し（半日）＋CPU配列の解放（半日）
- 真因B。案(a)＝メインのレンダラーでRTへ一度だけ焼く
- そのうえで `geometry.attributes` のCPU配列を解放。仕分けは調査済み：
  - 建設時のみ参照＝25箇所（解放前に完走するので安全）
  - **実行中に書くのは `THREE.Points` だけ**（`10318, 11602, 13280, 15179, 15189, 15247, 15381, 15413, 15426, 15929, 15944`）
    ＝ `compressStaticGeometry` の `isPoints` 除外をそのまま流用すれば足りる
  - 危なく見えて安全だったもの：`10345-10349` のRaycastは起動時1回のみ（`fishersInit` ガード）／`14358 warpRay` は `intersectObject` を呼んでいない／`19322` は `.count` のみ
  - **残る障害は「コンテキスト喪失からの復帰」**（threeはCPU配列から再アップロードする＝捨てると復帰しても世界が空）→ 第4段の復帰方式とセットで決める
  - **要追加調査**：`buildYatoFogGrid`（`6256`）の `setFromObject` に備え、解放前に全静的ジオメトリの boundingBox / Sphere を明示計算しておくこと
  - **過去に事故った領域。`npm run patrol` で全域撮影の裏取り必須**

### 第3段：常駐を減らす（約2日）
- `VRM_RES_KEEP` 20→8（`main.js:17593`）**※表示上限 CAP は8なのに20体保持している。ただし2026-07-10に意図して8→20へ上げた設定なので、「女の子がトゥーンに戻る」の再発に注意**
- 主人公VRMも焼き版に＋テクスチャのしきい値 1024→512（`main.js:17427`。非baked 7.1MB・1024pxのまま）
- 影マップ 2048→1024（−12〜24MB）
- `uv` Float32→Uint16、`index` Uint32→Uint16（−21MB。CPU+GPU両方で実質−42MB）
- Composer RGBA16F→RGBA8（−5.2MB）／`yatoGutter` 205,692頂点の削減（−5MB）
- 看板canvasの縮小（256×64が69枚・256×80が42枚）＋網テクスチャの重複統合（`9043` と `16840`）

### 第4段：落ちても痛くない・落ちたら気づく（約2日）
- **自動保存**：パンくず `hn3d_bc` に同居させる（位置・向き・エリア・`tday`・`day`・姿勢を1Hz・約500B・0.1ms未満）。進行（flags/caught/fish）はシグネチャの変化検知で `saveState()` を呼ぶ（6行・差し忘れゼロ）
- **つづきから**：落ちた時は黙って自動復帰／きれいに終わった時だけタイトルに「つづきから」を1つ。優先順位は 復帰 > `hn3d_spawn` > 既定。高さは保存値でなく `heightAt` で取り直す・屋上は復帰しない・`camSnap=true` 必須
- **正しいクラッシュ検知**：`hn3d_bc.ok===0` を見て「前面で急死した回数」だけを数える（今の `hn3d_bootn` を置き換え）
- **かるさの段（level）**：つまみを増やさず `?lowmem=0..4` ＋ `hn3d_level` の1本に統合。`__SAFE` は段3の別名にして既存コードを壊さない
  - 段1＝ほぼ気づかない（KEEP8／主人公512／マップの第2レンダラ禁止／影1024／主観DPRブースト停止）
  - 段2＝主役（＋旧プロト3エリア非構築・看板canvas・light相当）
  - **絶対に守る線＝谷戸の街並みの密度・時間と光・音・人が「いる」こと・おもいで帳**
- **段の上げ下げ**：1回で沈黙のまま+1／2回で一言／3回で安全モード／4回で避難画面。**下げ方も要る**＝クラッシュなしで通算10分遊べたら−1、下げ直後に落ちた段は24時間再挑戦しない、手動で段を選んだら自動は止める
- **`webglcontextlost` の作り直し**（`main.js:914-917`）：three.js r169 は自力で完全復帰できる（`onContextRestore` → `initGLContext()` で内部キャッシュを再生成し、次のrenderで自動再アップロード）。今の実装は**正しい復帰処理をreloadで潰している**。現実解＝**preventDefaultして待ち、8〜10秒来なければ初めてreload**。reload直前に必ず保存を挟む

### 第5段：再発防止（10行）
- **`renderer.info.memory` を体温計にする**：起動直後の枚数（実測149枚）をBASEとして記録し、5秒ごとに監視。
  2.2倍でKEEPを絞る／3.0倍でVRM全停止／3.5倍で段を上げて読み直す
- **この10行があれば、今回の漏れは「1体準備するたび+24枚、捨てても減らない」として実装当日に検知できた**

### 第6段：見た目を落とさない描画の最適化（発熱にも効く）

- **【最重要・省電力の目玉レバーが空振りしていた】`composer.setPixelRatio()` が一度も呼ばれていない**（grep 0件）。
  `_pixelRatio` が構築時の 1.30 で固定され、DPRを1.50/1.00に変えても composer のレンダーターゲットは `1211.6x559` のまま。
  → **軽量モード・中くらいモード（DPR 1.0）は、ポストと本編描画のフィルをまったく下げていない**（canvasだけ小さくなり、実質スーパーサンプリング）。
  → `resize()` に1行 `composer.setPixelRatio(dprNow())` を足すだけで、**軽量モードが −6.3MB ＋ フィル −41%** になる。**発熱対策として単独で最大の一手。**
  → 主観視点のDPR1.50ブースト（`main.js:900, 12268`）も実解像度を上げていないのに、出入りのたびRT再確保110回だけ起こしている
- **resize のレンダーターゲットの取りこぼしは「無い」（実証済み）**：DPR切替60回をサイズ別に突合し、`normalRT 751x347` 30/30、その深度テクスチャ 30/30、`867x400` 30/30、bloom各mip 183/183・122/122 と**全て確保＝解放で差0**。
  three r169 の `deallocateRenderTarget` が `depthTexture.dispose()` まで見ており、`main.js:12282-12288` の順序も正しい（12285付近の二重dispose呼びは冗長なだけで無害）。
  → **リークではなく churn（無駄な作り直し）だけが問題**。回転10往復18秒で確保252.94MB／解放247.44MB
- **GPU総量の実測（起動直後）＝160.08MB**：頂点/インデックス44.30 ＋ テクスチャ89.09（うち影マップ16.00）＋ 深度レンダーバッファ21.52 ＋ 既定FB 5.16／プログラム196本。
  **実プレイのピークは190〜295MB**（VRM遅延読込で+26〜63、ばしょマップで+71）。`?hq=1` はiPhoneでDPR3.0になり総量261MB（`start_url` はhq無しなので通常起動は安全）
- **VRMの遅延読込そのものはリークではない**（立っているだけで+4.6MB/10秒だが、対照区間で0.00MB＝頭打ちを確認）。**漏れるのは入れ替え（LRU churn）のとき**＝真因A

- `townNightLights`（`main.js:15405`）が **opacity を書くだけで `visible` を切っていない**
  → 昼のあいだ 121〜155個の半透明を無駄に描いている。しかも `yatoNightWindows0/1/2`・`yatoTVWindows`・`puddles`（`3023 / 3026 / 4999`）は**マージ済み＝フラスタムカリング不能＋DoubleSide＋fog:false＋depthWrite:false**＝iPhoneのTBDRで最も高くつく形。**直しても絵は完全に同一**
- `resize()` 1イベントで**RT再確保が110回**（`composer.setSize` がbloom11枚を実解像度で確保→次行がCSS/4に戻す二重呼び出し `12272-73` × `onResize` の5連発 `12299-305`）。主観視点の出入り（`18080`）・画面回転・設定変更のたびに発生
- `waterMat`（`1359`）は **sin 36回/画素**。うち20回（`n1`/`band`/`sp`）は `prox` 倍なので**70m超・浅い角度では厳密に0**。`if (prox > 0.001)` で括れば出力同一
- `postPass` の `fz`（`12235`）は全画素同値なのに毎画素で深度フェッチ → uniform化で67.7万フェッチ/フレーム削減
- 影の更新条件（`15041`）が Δtday>0.012 ＝**座ると時間が5倍速**（`sitFlow=5`・`15010`）なので1.5秒に1回、411drawcall/321k triの影パスが走る
- シェーダープログラムが遊んでいる最中に **187→254**（夕方だけで+51）＝`visible=false` の祭り／灯籠グループの初回描画。時刻またぎのフレームスパイクの原因

### 第7段：UX/UI（別紙 audit_5_ux.md に全件）
**不具合（重い順）**
1. 📷📔がモーダルより前面で押せる（z-index 38 対 guide 27 / settings 28 / diary 20）
2. **まどり（#plan）と⚙（#set-btn）が完全同座標**（`proto3d.html:45 / 254`・実測どちらも x=28, y=15）＝自宅の中で⚙が押せない
3. 「あそびかたを みる」が実質たどり着けない（`proto3d.html:313`。全開時 y=560＝画面外）。初回ガイドは1回きり
4. 写真モードだけセーフエリア未対応（`photo.js:120` の `#pm-view-bar{bottom:5vh}` がホームインジケータ帯に重なる／`photo.js:78` の `#pm-tools` は右ノッチに入り高さ21px）
5. `prefers-reduced-motion` の停止漏れ4件／タップ44px未満4件（`#set-x` 34 / `#mb-close` 40 / `.mb-tab` 37 / `#pm-quality` 21）
6. standalone にリロード手段が無い＝せってい に「開き直す」を1つ

**体験を上げる小さな追加**
- **「じゅんびちゅう」に進捗**（`__breathN`／37節を既に持っている）。実測は初回96秒・2回目36.8秒（ヘッドレス）で、**待ちの正体はダウンロードではなくシーンの建設**＝本体DLでは速くならない
- ローディングの覆いが冷たい青灰色（`proto3d.html:241`）→ 夏の色へ。毎回いちばん長く見る絵
- `#badge` に場所を1語（`nearPlace()` が既存・`main.js:14176`）。地図は出さない
- パッド接続トーストに「Menu＝せってい／View＝割当」。キーボード操作が薄い（Space=ジャンプ、Enter=はじめる のみ）

---

## 8. やらないと決めたこと

- **エリア分割読み込み＝今はやらない**。真因が漏れだったので不要な公算が高い／同じ削減額を1/5以下の工数で得られる／`colliders`・`cgrid` が index 方式で途中削除するとグリッドが全ずれ（`162-171`）、`buildShishigaya` 3400行に区画単位の入口が無い（`2681-6104`）、NPCは全員ぶんを毎フレーム舐める設計＝実質エンジンの書き直し／道の当たり判定と接地という最も事故る領域を最も広く触る。**再検討は第1〜3段を全部やっても落ちる場合のみ**
- WebGPU移行・ゲームエンジン移行（決着済み）
- 動的解像度の既定ON（チラつきの主因として一度切った）
- `?hq=1` をiPhone単体へ開放（RTが56→157MBに膨らむ）
- grade削減／DOFタップ8→6／godray 24→16以下／composerの8bit化／半透明の一律間引き／プリパス0.62以下（いずれも計測で決着済み）
- `public/sw.js` のキャッシュ戦略変更（HTMLキャッシュ事故の再発リスク）
- ばしょマップの常設表示（「当てもなく歩く」を壊す）／HUDへの数字バッジ

---

## 9. 前任3手（2026-07-22）の答え合わせ

| 手 | 評価 |
|---|---|
| 頂点属性の圧縮（272→188MB） | **○ 残す**。−85.3MB は実測で効いている。的は外れたが無駄ではない |
| 息継ぎ `__breath`（約30箇所） | **△** メモリのピークは1バイトも下がらない。狙った「無応答→強制終了」自体が起きていなかった（タイトルが出る＝建設は完走している） |
| `warmGpuStaged`（段階コンパイル） | **△〜×** 初回描画前に全プログラムを作る＝**開始時点のGPU占有を先に最大化**しており、後から積む型では不利 |
| 避難画面 | コードは正しく到達可能。**引き金だけが死んでいた** |

**総括：「軽く・分けて・逃がす」の発想は妥当。外れたのは"いつ死ぬか"の見立てひとつ。**

---

## 10. 実機での確かめ方（作者向け）

せってい → 🛠 → 「かずを 画面に 出す」を ON にして、次の順路を歩く。

1. その場で30秒（基準）
2. 商店街へ歩く
3. 商店街を2往復
4. 小学校の校庭へ
5. 校庭で60秒
6. **二ツ池まで戻る**（＝準備済みVRMが全員追い出される距離。**ここで枚数が戻るかが核心**。今は戻らないはず）

**合格線**：⑤で350〜400枚で頭打ち／⑥で「まいご」0〜20枚／戻りが200枚以上。
修正の前後で同じ道順を1回ずつ。

**併せてやるA/B**（どれもコストゼロ）
- ホーム画面のアイコン版 vs Safariのタブ版
- 安全モード ON / OFF（VRM全停止＝漏れの供給源がゼロになる）
- 主観視点を10回出入り＋画面を5回回転（`rs` / `rt` の増え方を見る）

**原因ログを自分で取る**：設定 → プライバシーとセキュリティ → 解析と改善 → 解析データ。
`JetsamEvent-*.ips` があれば**メモリで殺された**。開いて `largestProcess` が `com.apple.WebKit.WebContent` か確認し、`processes` 配列で `reason` キーが付いているものを探す（`per-process-limit`＝自分の使いすぎ／`vm-pageshortage`＝システム全体の逼迫）。
**`rpages × 16384バイト` ＝ 使用バイト数**。6GB機の目安：rpages 125,000前後（≒2.0GB）で上限到達、80,000（≒1.25GB）超で黄信号。
`panic-full-*` があれば端末の再起動（今回は無いはず＝**無いことの確認**が大事）。

---

## 11. 調査の元資料

各班の詳細報告（このセッションの作業フォルダ）：

| 役割 | ファイル |
|---|---|
| 1. iOS/WebKitの最新情報 | `audit_1_ios.md` |
| 2. メモリの実測プロファイル（493行） | `audit_2_memprofile.md` |
| 3. メモリ消費のコード監査 | `audit_3_memcode.md` |
| 4. 描画パイプライン／発熱 | `audit_4_render.md` |
| 5. UX/UI | `audit_5_ux.md` |
| 6. 起動シーケンス／縮退設計 | `audit_6_boot.md` |
| 7. 実機診断キット／自動保存の設計 | `audit_7_diag.md` |

再利用できる計測スクリプト（`scripts/_*.mjs`・gitignore済み）：
`_runmem6.mjs`（遊んでいる最中の増え方・カウンタ実測）、`_areamem6.mjs`（エリア別ジオメトリ：旧プロト74.7MB／神社23.1MB／谷戸87.3MB）

---

## 12. 本人の判断（2026-07-25・確認済み）

| 論点 | 決定 |
|---|---|
| 次の /goal の範囲 | **全部（第0段〜第7段。描画最適化・UI修正まで）** |
| 旧プロト3エリア（実写 `.verify/oldarea_*.png` を見たうえで確定） | 下表のとおり |

### 旧プロト3エリアの扱い（確定・2026-07-25）

| エリア | 幾何 | 決定 | 備考 |
|---|---:|---|---|
| **はらっぱ**（縁側の家）<br>x −50〜50 / z −20〜60 | 21.3MB | **残す（全環境）** | 縁側のおばあちゃん・風鈴・洗濯物・よしず・朝顔の垣根・たらいの西瓜・金魚鉢・ねこ・畑。**3エリアで唯一、谷戸と同じ密度**。作者の原風景。**絶対に消さない** |
| **最初の町＋裏山**<br>x 650〜1100 / z −345〜230 | 55.4MB | **iPhone単体の時だけ作らない** | 3つで最大。西半分は建物ゼロの更地・地面は単色・住人は旧世代の造形。**PC／リモートコントロールでは今まで通り歩ける**。可逆 |
| **神社**（旧・重複）<br>x 1945〜2055 / z −52〜88 | 24.3MB | **どの端末でも作らない** | **実測で画面に一度も出ていない**（`cullAreas()` が bbox中心 x>1000 を谷戸側と仕分けるため、神社エリアに入ると地面ごと丸ごと非表示。`_cullCounts()` → `yShown:0`、実写は空と雲だけ）。谷戸の神明社と重複。**実装前に非表示を再確認してから外すこと** |

**iPhone単体での削減＝79.7MB／全環境での削減＝24.3MB。**
| 連続で落ちたときの自動縮退 | **一言たずねてから軽くする**（1回目は黙って少しだけ／2回目で「少しかるくしてもいい？」と聞く／さらに続けば安全モード）。**勝手に見た目を変えない** |
| 落ちて読み込み直された時 | **黙って続きから戻る**（場所・時刻・持ち物のまま再開。きれいに終わった時だけタイトルに「つづきから」を1つ出す） |

### まだ確認が要る点
- **`VRM_RES_KEEP` 20→8** は「女の子がトゥーンに戻る」の再発リスクがある（2026-07-10に意図して8→20へ上げた設定）。
  → **真因Aを止めた後の実測を見てから**判断する。漏れが止まれば20のままで足りる可能性がある
- **見た目が少し変わる項目**（影マップ1024／主人公VRMのテクスチャ512）は、**iPhone単体の時だけ**適用する方針で進める（リモートコントロールとPCは現状維持）。異論があれば差し戻す

---

## 13. 推奨する実行順（/goal 用）

1. **第0段 診断キット**（141行）— これが無いと「直ったか」を実機で判定できない。最初に出す
2. **第1段 漏れ止め**（数行）— 真因A
3. **ばしょマップ 応急3行**（`glr.dispose()` / `forceContextLoss()` / `null`）— 真因B・見た目の犠牲ゼロ
4. **`composer.setPixelRatio(dprNow())` 1行** — 軽量モードのフィル −41%。発熱対策として単独で最大
5. ここで**いったんデプロイし、作者に実機で確かめてもらう**（第10節の順路）。ここまでで落ちなくなる可能性が高い
6. 第2段 ばしょマップ根治（案a）＋ CPU配列解放
7. 第4段 自動保存・つづきから・正しいクラッシュ検知・かるさの段
8. 第5段 体温計（10行）
9. 第6段 残りの描画最適化（夜の灯り／resize churn／水シェーダー／postPass／影の更新頻度）
10. 第7段 UI修正（同座標ボタン → z-index → あそびかたの導線 → 写真モードのセーフエリア → 起動の進捗表示）
11. 第3段 常駐削減（旧プロト3エリアの扱いは第12節で確定済み＝はらっぱは残す／町＋裏山はiPhoneのみ非構築／神社は全環境で非構築）

**各段の締めに `npm run verify` → `npm run patrol` → `npm run playcheck` → デプロイ確認（CI success ＋ 本番JSハッシュ変化）。**
**メモリに関わる修正は、必ず修正前後で「まいご（孤児）テクスチャの枚数」を実測して数字で示すこと。**

---

## 14. 実施の結果（2026-07-25・全段デプロイ済）

| 段 | 実測の結果 |
|---|---|
| 第0段 数え板・パンくず | せってい→🛠→「かずを 画面に 出す」で稼働。ok印・急死の検知・前回の記録すべて動作確認 |
| 第1段 漏れ止め | 同条件（準備済5体・生きている枚数635）で **2周目342枚→3周目342枚＝±0**。修正前は 327→373（+46） |
| 第2段 マップ根治＋CPU配列解放 | 2回目以降の焼き直し ±0。全エリア常駐ヒープ **430.7→352.2MB**。解放量62.4MB／2916本 |
| 第3段 古いエリアを手放す | 神社＝全環境（1051群/666かたち）。町＋裏山＝スマホのみ。**パソコン379MB→スマホ318MB（−61MB）** |
| 第4段 続きから・かるさの段 | 黙って復帰／つづきから／2回目の相談＝すべて実測で動作 |
| 第5段 体温計 | 2.3倍→保有6／3.1倍→人シンプル／3.6倍→段+1で自分から畳む＝すべて発火 |
| 第6段 描画 | 回転10回のバッファ作り直し **2240→260回（−88%）**、主観の出入り **110→0回**。絵は同一（patrol16枚の差はすべて同ビルド2回のゆらぎの範囲内） |
| 第7段 UI | 重なり・同座標・画面外・押せる大きさ＝実測で全項目○ |

### 当初の見積りからの訂正
- 「漏れ +111枚/周」は**計測側の数え落とし**（MToonの`map`等はクラスのアクセサで`for...in`に出てこない）による過大読み。正しくは上表のとおり。
- CPU配列の解放量は 191.8MB でなく **62.4MB**（頂点圧縮で既に85MB削った後の残りぶん＋粒子/人/小さいかたちを対象外にしたため）。

### やらないと決めた（追加）
- **影の更新条件（`Δtday>0.012`／座ると時間5倍）**：間隔を空けると、座って夕焼けを眺めるまさにその場面で影が飛んで見える。この作品でいちばん守るべき時間なので節電と釣り合わない。
- **`VRM_RES_KEEP` 20→8**：漏れを止めた後の実測で頭打ちを確認したため**20のまま据え置き**（かるさの段1以上でだけ8）。

### 実写で見つけた巻き込み事故（机上では気づけなかった・恒久の教訓）
1. CPU配列解放で**顔ゲートが壊れた**＝人のかたちは使い回されるため、`userData.head`を持つ枝と頂点256未満は対象外に。
2. エリアを手放す判定を「中心が範囲内」にすると**共有の大地面を巻き込み、はらっぱが真っ暗**に→「まるごと範囲に収まっている物だけ」へ。
3. **粒子(Points)のかたちを手放すと毎フレーム落ちる**（シーンから外しても更新処理は動き続ける）。
4. **はらっぱは町を通らないと行けない**→町を作らない時は谷戸の門を はらっぱ へ直につなぎ替え、道標も書き換えた。
