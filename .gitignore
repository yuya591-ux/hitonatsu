# 依存パッケージ
node_modules/

# 確認用に落としたChrome for Testing（Edge更新に左右されないheadless用・巨大なのでGit対象外）
chrome/

# ビルド成果物
dist/

# ログ・キャッシュ
*.log
.vite/

# 自己検証の出力（スクリーンショット等）
.verify/

# 使い捨ての評価スクショ（ルート直下のpng・評価用フォルダ。リポジトリに残さない）
/*.png
eval_shots/
evalart/

# 一時検証スクリプト（アンダースコア始まり＝使い捨て）
scripts/_*.mjs

# APIキー類（外部AI生成は開発時のみ。鍵は絶対にGitに上げない）
.env
.env.*
.pollinations_key
*.key

# OS・エディタの作業ファイル
.DS_Store
Thumbs.db
.vscode/
.idea/
